// Getting length of word using "word.length"
var myName = "Naveen";
console.log(myName.length);

// String Slicing using "stringName.slice(startIndex, endIndex)"
console.log(myName.slice(2, 5));

// Using "stringName.toUpperCase()"
var surName = "sharma";
console.log(surName.toUpperCase());
